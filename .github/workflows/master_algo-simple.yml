# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Node.js app to Azure Web App - Algo-Simple

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:

  envars:
    runs-on: ubuntu-latest
    needs: build
    steps:
    - name: Ecoloco
      run: echo $(SOKETIN)

    - name: Ecoloco2
      run: echo $SOKETON

    - name: Ecoloco
      run: echo ${{ secret.SOKETIN }}
